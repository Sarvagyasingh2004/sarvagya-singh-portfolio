GEMINI_API_KEY=
PORT=3001
NODE_ENV=development
GEMINI_MODEL=gemini-2.5-flash
CHATBOT_ENABLED=true
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
IP_HASH_SALT=
# Optional — the server runs fine without these.
MONGODB_URI=
AWS_REGION=ap-south-1
RESUME_BUCKET=
SES_FROM=
NOTIFY_EMAIL=
